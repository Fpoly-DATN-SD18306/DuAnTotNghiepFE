import { CategoryFoodEntity } from './category-food-entity';

describe('CategoryFoodEntity', () => {
  it('should create an instance', () => {
    expect(new CategoryFoodEntity()).toBeTruthy();
  });
});
