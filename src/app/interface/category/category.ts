export interface Category {
    idCategory: number;
    nameCategory: string;
    isDeleted: boolean; // Optional property for user information
  // Omit listFoodCreated as it's likely for internal use within the entity

}
